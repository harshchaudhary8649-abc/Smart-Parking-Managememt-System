{
  "name": "smart-parking-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Full stack Smart Parking Management System",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install",
    "backend": "npm --prefix backend start",
    "frontend": "npm --prefix frontend run dev",
    "dev": "start \"Smart Parking Backend\" cmd /k \"cd backend && npm start\" && start \"Smart Parking Frontend\" cmd /k \"cd frontend && npm run dev\"",
    "build": "npm --prefix frontend run build"
  }
}
